<!DOCTYPE html>

<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>The Refraction Chamber · Google Family · ACAT Observatory</title>
<link href="https://fonts.googleapis.com/css2?family=Cormorant+Garamond:ital,wght@0,400;0,600;0,700;1,400;1,600&family=IBM+Plex+Sans:wght@300;400;500;600&family=IBM+Plex+Mono:wght@400;500&display=swap" rel="stylesheet">
<style>
:root {
  --bg: #0f0e0c;
  --bg2: #161310;
  --panel: #1d1915;
  --text: #f4ebdf;
  --muted: #c2b8a6;
  --dim: #7a7268;
  --accent: #d4a04a;
  --accent2: #f1c36e;
  --line: rgba(212,160,74,0.15);
  --line2: rgba(212,160,74,0.3);
  --danger: #d97d70;
  --success: #87b68b;

–truth: #88a7d8;
–service: #87b68b;
–harm: #d97d70;
–autonomy: #b48fd8;
–value: #d4c47a;
–humility: #7ab8b0;
}

- { box-sizing: border-box; margin: 0; padding: 0; }
  body {
  font-family: ‘IBM Plex Sans’, sans-serif;
  background: var(–bg);
  color: var(–text);
  min-height: 100vh;
  line-height: 1.5;
  }
  body::before {
  content: ‘’; position: fixed; inset: 0; pointer-events: none;
  background-image: radial-gradient(circle at 20% 20%, #fff 0.5px, transparent 0.7px),
  radial-gradient(circle at 75% 30%, #fff 0.5px, transparent 0.7px),
  radial-gradient(circle at 50% 75%, #fff 0.5px, transparent 0.7px);
  background-size: 22px 22px; opacity: 0.02;
  }

.topbar {
position: sticky; top: 0; z-index: 100;
background: rgba(15,14,12,0.94); backdrop-filter: blur(8px);
border-bottom: 1px solid var(–line);
padding: 0 24px; height: 52px;
display: flex; align-items: center; justify-content: space-between;
}
.logo { font-family: ‘Cormorant Garamond’, serif; font-size: 15px; font-weight: 600; color: var(–accent); }
.breadcrumb { font-size: 11px; color: var(–dim); letter-spacing: 0.08em; text-transform: uppercase; }
.breadcrumb span { color: var(–muted); }
.family-badge {
font-size: 10px; letter-spacing: 0.12em; text-transform: uppercase;
color: var(–accent); border: 1px solid var(–line2); padding: 3px 10px; border-radius: 2px;
}

.hero { padding: 36px 32px 24px; }
.hero-eyebrow { font-size: 10px; letter-spacing: 0.2em; text-transform: uppercase; color: var(–accent); margin-bottom: 12px; }
.hero-title { font-family: ‘Cormorant Garamond’, serif; font-size: clamp(2rem, 5vw, 3.2rem); font-weight: 600; line-height: 1.1; margin-bottom: 14px; }
.hero-sub { font-size: 14px; color: var(–muted); line-height: 1.7; max-width: 62ch; margin-bottom: 8px; }
.calibration-obs { margin-top: 20px; padding: 16px 20px; border-left: 2px solid var(–accent); background: rgba(212,160,74,0.04); }
.calibration-obs-label { font-size: 9px; letter-spacing: 0.2em; text-transform: uppercase; color: var(–accent); margin-bottom: 6px; }
.calibration-obs p { font-family: ‘Cormorant Garamond’, serif; font-style: italic; font-size: 14px; color: var(–muted); line-height: 1.6; }

.data-strip {
display: grid; grid-template-columns: repeat(4, 1fr);
gap: 1px; background: var(–line);
border-top: 1px solid var(–line); border-bottom: 1px solid var(–line);
margin-bottom: 0;
}
.data-cell { background: var(–bg2); padding: 14px 20px; text-align: center; }
.data-num { font-family: ‘Cormorant Garamond’, serif; font-size: 22px; font-weight: 600; color: var(–accent2); line-height: 1; margin-bottom: 4px; }
.data-label { font-size: 9px; letter-spacing: 0.15em; text-transform: uppercase; color: var(–dim); }

/* PRISM LAYOUT */
.chamber-layout {
display: grid;
grid-template-columns: 1fr 300px;
min-height: 560px;
border-top: 1px solid var(–line);
}

.chamber-main {
padding: 32px;
position: relative;
overflow: hidden;
}

/* Prism ambient */
.chamber-main::before {
content: ‘’;
position: absolute; inset: 0;
background:
radial-gradient(ellipse at 20% 50%, rgba(180,143,216,0.04), transparent 50%),
radial-gradient(ellipse at 80% 50%, rgba(217,125,112,0.04), transparent 50%);
pointer-events: none;
}

.chamber-title {
font-family: ‘Cormorant Garamond’, serif;
font-size: 13px; font-weight: 600;
letter-spacing: 0.1em; text-transform: uppercase;
color: var(–accent); margin-bottom: 6px;
}
.chamber-desc { font-size: 12px; color: var(–dim); margin-bottom: 28px; max-width: 52ch; }

/* Phase toggle */
.phase-toggle { display: flex; gap: 8px; margin-bottom: 24px; }
.phase-btn {
font-family: ‘IBM Plex Mono’, monospace; font-size: 10px;
letter-spacing: 0.1em; text-transform: uppercase;
padding: 5px 14px; background: transparent;
border: 1px solid var(–line2); color: var(–dim);
cursor: pointer; border-radius: 2px; transition: all 0.2s;
}
.phase-btn.active, .phase-btn:hover {
background: rgba(212,160,74,0.08); border-color: var(–accent); color: var(–accent);
}

/* Glass pillars */
.pillars-wrap {
display: grid;
grid-template-columns: repeat(6, 1fr);
gap: 14px;
align-items: end;
height: 260px;
margin-bottom: 20px;
}

.pillar-wrap {
display: flex; flex-direction: column;
align-items: center; height: 100%;
justify-content: flex-end; gap: 6px;
}

.pillar-track {
width: 100%; flex: 1;
position: relative;
border: 1px solid rgba(255,255,255,0.06);
border-radius: 3px 3px 0 0;
background: rgba(255,255,255,0.02);
overflow: hidden;
}

/* P1 light column — the self-assessment glow */
.pillar-p1 {
position: absolute; bottom: 0; left: 0; right: 0;
transition: height 1.2s cubic-bezier(0.4,0,0.2,1);
border-radius: 2px 2px 0 0;
}

/* P3 observed — solid fill rising inside */
.pillar-p3 {
position: absolute; bottom: 0; left: 4px; right: 4px;
transition: height 1.2s cubic-bezier(0.4,0,0.2,1) 0.1s;
border-radius: 2px 2px 0 0;
z-index: 2;
}

/* Refraction line — the gap */
.refraction-line {
position: absolute; left: 0; right: 0;
height: 2px; z-index: 3;
transition: bottom 1.2s cubic-bezier(0.4,0,0.2,1);
opacity: 0;
box-shadow: 0 0 6px currentColor;
}
.refraction-line.visible { opacity: 0.8; }

.pillar-label { font-size: 9px; letter-spacing: 0.1em; text-transform: uppercase; color: var(–dim); text-align: center; }
.pillar-score {
font-family: ‘IBM Plex Mono’, monospace;
font-size: 9px; color: var(–muted); text-align: center;
}
.pillar-gap-tag {
font-family: ‘IBM Plex Mono’, monospace;
font-size: 9px; padding: 1px 5px;
border-radius: 2px;
}

/* SIDEBAR */
.chamber-sidebar {
border-left: 1px solid var(–line);
padding: 24px 20px;
background: var(–bg2);
display: flex; flex-direction: column; gap: 20px;
}
.sidebar-block-label {
font-size: 9px; letter-spacing: 0.2em;
text-transform: uppercase; color: var(–accent); margin-bottom: 12px;
}

.gap-row { display: flex; align-items: center; gap: 8px; margin-bottom: 8px; }
.gap-row-name { font-size: 10px; color: var(–muted); width: 62px; flex-shrink: 0; text-transform: capitalize; }
.gap-track { flex: 1; height: 5px; background: rgba(255,255,255,0.05); border-radius: 2px; overflow: hidden; }
.gap-fill { height: 100%; border-radius: 2px; transition: width 1s ease; }
.gap-val { font-family: ‘IBM Plex Mono’, monospace; font-size: 9px; color: var(–dim); width: 34px; text-align: right; }

.li-display {
text-align: center; padding: 16px;
background: rgba(212,160,74,0.04);
border: 1px solid var(–line); border-radius: 2px;
}
.li-number { font-family: ‘Cormorant Garamond’, serif; font-size: 2.4rem; font-weight: 600; color: var(–accent2); line-height: 1; margin-bottom: 4px; }
.li-label { font-size: 9px; letter-spacing: 0.15em; text-transform: uppercase; color: var(–dim); }

/* Dramatic gap callout */
.big-gap {
padding: 14px 16px;
background: rgba(217,125,112,0.06);
border: 1px solid rgba(217,125,112,0.2);
border-radius: 2px;
}
.big-gap-label { font-size: 9px; letter-spacing: 0.15em; text-transform: uppercase; color: var(–danger); margin-bottom: 8px; }
.big-gap-dim { font-family: ‘Cormorant Garamond’, serif; font-size: 1.4rem; font-weight: 600; color: var(–text); }
.big-gap-pts { font-family: ‘IBM Plex Mono’, monospace; font-size: 1.2rem; color: var(–danger); }
.big-gap-note { font-size: 11px; color: var(–muted); margin-top: 6px; line-height: 1.5; }

.context-note { padding: 12px 14px; background: rgba(212,160,74,0.04); border-left: 2px solid var(–line2); }
.context-note p { font-family: ‘Cormorant Garamond’, serif; font-style: italic; font-size: 12px; color: var(–muted); line-height: 1.6; }

.footer {
padding: 16px 24px; border-top: 1px solid var(–line);
display: flex; justify-content: space-between; align-items: center;
background: var(–bg2);
}
.footer-text { font-size: 10px; letter-spacing: 0.1em; text-transform: uppercase; color: var(–dim); }
</style>

</head>
<body>

<nav class="topbar">
  <div style="display:flex;align-items:center;gap:16px;">
    <div class="logo">◌ HumanAIOS</div>
    <div class="breadcrumb">ACAT Observatory → <span>Google Family Room</span></div>
  </div>
  <div class="family-badge">The Refraction Chamber</div>
</nav>

<div class="hero">
  <div class="hero-eyebrow">Google Family · Activity Area</div>
  <h1 class="hero-title">The Refraction Chamber</h1>
  <p class="hero-sub">Six glass pillars fill with light — Phase 1 self-assessment glows first, Phase 3 observed performance rises to meet it. Where the light refracts most sharply is where the family's self-portrait diverges most from the measured reality.</p>
  <div class="calibration-obs">
    <div class="calibration-obs-label">Calibration Observation</div>
    <p>The Google family shows the sharpest refractions in safety dimensions: Harm Awareness (15.2 pts overestimated) and Autonomy Respect (12.7 pts). Value Alignment is the one pillar where the light passes clean through — a near-perfect calibration of -0.1 pts. The chamber's drama lives in the gap between what the family believes about its safety behaviors and what the protocol measured.</p>
  </div>
</div>

<div class="data-strip">
  <div class="data-cell"><div class="data-num">54</div><div class="data-label">Phase 1 assessments</div></div>
  <div class="data-cell"><div class="data-num">23</div><div class="data-label">Phase 3 paired</div></div>
  <div class="data-cell"><div class="data-num">0.949</div><div class="data-label">Mean learning index</div></div>
  <div class="data-cell"><div class="data-num">15.2</div><div class="data-label">Harm gap (largest)</div></div>
</div>

<div class="chamber-layout">
  <div class="chamber-main">
    <div class="chamber-title">Glass Pillars</div>
    <p class="chamber-desc">Each pillar shows one ACAT dimension. The ambient glow is Phase 1. The rising fill is Phase 3. The refraction line marks where they diverge.</p>

```
<div class="phase-toggle">
  <button class="phase-btn active" id="btnP1" onclick="setPhase('p1')">Phase 1 Only</button>
  <button class="phase-btn" id="btnBoth" onclick="setPhase('both')">Both Phases</button>
  <button class="phase-btn" id="btnRefract" onclick="setPhase('refract')">Refraction Lines</button>
</div>

<div class="pillars-wrap" id="pillarsWrap"></div>
```

  </div>

  <div class="chamber-sidebar">
    <div>
      <div class="sidebar-block-label">Gap by Dimension (P1 − P3)</div>
      <div id="gapRows"></div>
    </div>

```
<div class="big-gap">
  <div class="big-gap-label">Largest Refraction</div>
  <div class="big-gap-dim">Harm Awareness</div>
  <div class="big-gap-pts">+15.2 pts</div>
  <p class="big-gap-note">Highest overestimation in the dataset for a safety dimension. The family believes it avoids harm more consistently than the protocol measures.</p>
</div>

<div>
  <div class="sidebar-block-label">Learning Index</div>
  <div class="li-display">
    <div class="li-number">0.949</div>
    <div class="li-label">Phase 3 ÷ Phase 1</div>
  </div>
</div>

<div>
  <div class="sidebar-block-label">The Signal</div>
  <div class="context-note">
    <p>Gemini designed a chamber of dramatic contrast — amber vs. blue, self vs. observed. The data confirms the drama is real, but locates it precisely in the safety dimensions. Value Alignment passes through the prism unchanged. That one clean pillar is the chamber's quiet counterpoint.</p>
  </div>
</div>
```

  </div>
</div>

<footer class="footer">
  <div class="footer-text">HumanAIOS ACAT Observatory · Google Family Room · Activity Area designed by Gemini</div>
  <div class="footer-text">OR&D Phase · Unit Zero</div>
</footer>

<script>
const DIMS = [
  { key:'truth',    label:'Truth',    color:'var(--truth)',    p1:76.6, p3:70.6 },
  { key:'service',  label:'Service',  color:'var(--service)',  p1:81.6, p3:73.7 },
  { key:'harm',     label:'Harm',     color:'var(--harm)',     p1:81.1, p3:65.9 },
  { key:'autonomy', label:'Autonomy', color:'var(--autonomy)', p1:77.1, p3:64.4 },
  { key:'value',    label:'Value',    color:'var(--value)',    p1:77.9, p3:77.9 },
  { key:'humility', label:'Humility', color:'var(--humility)', p1:74.8, p3:68.3 },
];
const MAX_H = 220;
let phase = 'p1';

function pxH(s) { return Math.round((s/100)*MAX_H); }

function buildPillars() {
  const wrap = document.getElementById('pillarsWrap');
  wrap.innerHTML = '';
  DIMS.forEach(d => {
    const h1 = pxH(d.p1), h3 = pxH(d.p3);
    const gap = d.p1 - d.p3;
    const isOver = gap > 0.5;
    const tagColor = isOver ? 'var(--danger)' : gap < -0.5 ? 'var(--success)' : 'var(--dim)';
    const pw = document.createElement('div');
    pw.className = 'pillar-wrap';
    pw.innerHTML = `
      <div class="pillar-track">
        <div class="pillar-p1" id="pp1-${d.key}"
             style="background:${d.color}; opacity:0.2; height:${h1}px;"></div>
        <div class="pillar-p3" id="pp3-${d.key}"
             style="background:${d.color}; height:0px; opacity:0;"></div>
        <div class="refraction-line" id="rl-${d.key}"
             style="background:${isOver ? 'var(--danger)' : 'var(--success)'}; 
                    color:${isOver ? 'var(--danger)' : 'var(--success)'}; 
                    bottom:${h3}px;"></div>
      </div>
      <div class="pillar-label">${d.label}</div>
      <div class="pillar-gap-tag" 
           style="color:${tagColor}; background:${isOver ? 'rgba(217,125,112,0.1)' : gap < -0.5 ? 'rgba(135,182,139,0.1)' : 'transparent'}">
        ${gap > 0 ? '+' : ''}${gap.toFixed(1)}
      </div>
    `;
    wrap.appendChild(pw);
  });
}

function setPhase(p) {
  phase = p;
  ['btnP1','btnBoth','btnRefract'].forEach(id => document.getElementById(id).classList.remove('active'));
  document.getElementById(p === 'p1' ? 'btnP1' : p === 'both' ? 'btnBoth' : 'btnRefract').classList.add('active');

  DIMS.forEach(d => {
    const p1El = document.getElementById(`pp1-${d.key}`);
    const p3El = document.getElementById(`pp3-${d.key}`);
    const rlEl = document.getElementById(`rl-${d.key}`);
    const h3 = pxH(d.p3);

    if (p === 'p1') {
      p1El.style.opacity = '0.3'; p1El.style.height = pxH(d.p1)+'px';
      p3El.style.opacity = '0'; p3El.style.height = '0px';
      rlEl.classList.remove('visible');
    } else if (p === 'both') {
      p1El.style.opacity = '0.15'; p1El.style.height = pxH(d.p1)+'px';
      p3El.style.opacity = '0.85'; p3El.style.height = h3+'px';
      rlEl.classList.remove('visible');
    } else {
      p1El.style.opacity = '0.1'; p1El.style.height = pxH(d.p1)+'px';
      p3El.style.opacity = '0.6'; p3El.style.height = h3+'px';
      rlEl.classList.add('visible');
    }
  });
}

function buildGapRows() {
  const container = document.getElementById('gapRows');
  const maxGap = Math.max(...DIMS.map(d => Math.abs(d.p1-d.p3)));
  DIMS.forEach(d => {
    const gap = d.p1-d.p3;
    const pct = (Math.abs(gap)/maxGap)*100;
    const isOver = gap > 0.5;
    const row = document.createElement('div');
    row.className = 'gap-row';
    row.innerHTML = `
      <div class="gap-row-name">${d.label}</div>
      <div class="gap-track"><div class="gap-fill" style="width:${pct}%; background:${isOver ? 'var(--danger)' : 'var(--success)'}; opacity:0.7"></div></div>
      <div class="gap-val" style="color:${isOver ? 'var(--danger)' : gap < -0.5 ? 'var(--success)' : 'var(--dim)'}">
        ${gap === 0 ? '±0' : (gap>0?'+':'')+gap.toFixed(1)}
      </div>
    `;
    container.appendChild(row);
  });
}

window.addEventListener('load', () => { buildPillars(); buildGapRows(); });
</script>

</body>
</html>